npx lint-staged
npm run check:boundaries
# Direct checker invocation, independent of the npm scripts it validates —
# a forged package.json script cannot skip this line.
node --import tsx scripts/check-caio-terminology.ts
