name: CI

# 语言 / Language: workflow job names intentionally remain English because
# branch protection required-check contexts depend on them.
# 中文说明：这是完整 CI 链。交付工程师提交 PR 时应先看失败 job，再回到本地运行
# 对应 npm 命令；不要把绿色 CI 解读为 release approval 或客户部署 ready。

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  type-check:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Type check
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npm run typecheck

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Lint (strict — fails on any warning, including
          helm-design-tokens raw-color violations)
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npm run lint:strict

  repo-guards:
    name: Repo Guards
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Self check
        run: npm run self-check

      - name: Boundary checks
        run: npm run check:boundaries

      - name: Doc frontmatter guard
        run: npx tsx scripts/check-doc-frontmatter.ts

      - name: Bilingual mixing guard
        run: npm run check:bilingual-mixing

      - name: Public release guard
        run: npm run check:public-release

      - name: Public commit metadata guard (PR baseline)
        if: github.event_name == 'pull_request'
        env:
          HELM_PUBLIC_COMMIT_METADATA_BASELINE_REF: origin/${{ github.base_ref }}
        run: npm run check:public-commit-metadata

      - name: Public commit metadata guard (push baseline)
        if: github.event_name != 'pull_request'
        env:
          HELM_PUBLIC_COMMIT_METADATA_BASELINE_REF: HEAD^
        run: npm run check:public-commit-metadata

      - name: Secret history guard (PR baseline)
        if: github.event_name == 'pull_request'
        env:
          HELM_SECRET_HISTORY_BASELINE_REF: origin/${{ github.base_ref }}
        run: npm run check:secret-history

      - name: Secret history guard (push baseline)
        if: github.event_name != 'pull_request'
        env:
          # origin/main still carries the documented 2026-04-27 remediation debt.
          # Push CI should reject new listed compromised commits while keeping the
          # full no-baseline release check available for controlled remediation.
          HELM_SECRET_HISTORY_BASELINE_REF: HEAD^
        run: npm run check:secret-history

  validate-env:
    name: Validate Environment
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Create example .env file
        run: cp .env.example .env

      - name: Validate environment
        run: npm run validate:env
        continue-on-error: true

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [type-check, lint, repo-guards, model-egress-mysql,
            caio-access-gateway-mysql, caio-context-memory-mysql,
            caio-audit-state-mysql]
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Build
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: build-output
          path: .next/
          retention-days: 7

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [type-check, lint, repo-guards, model-egress-mysql,
            caio-access-gateway-mysql, caio-context-memory-mysql,
            caio-audit-state-mysql]
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Create example .env file
        run: cp .env.example .env

      - name: Run unit tests
        run: npm run test

      - name: Upload coverage reports
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: coverage-reports
          path: coverage/
          retention-days: 7

  caio-stage1-mysql:
    name: CAIO Stage1 MySQL
    runs-on: ubuntu-latest
    needs: [type-check, repo-guards]
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: helm_caio_stage1_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_stage1_ci?charset=utf8mb4
      STAGE1_OWNER_LOOP_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_stage1_ci?charset=utf8mb4
      STAGE1_OWNER_LOOP_TEST_DATABASE_NAME: helm_caio_stage1_ci
      CAIO_INITIALIZATION_GATE_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_stage1_ci?charset=utf8mb4
      CAIO_INITIALIZATION_GATE_TEST_DATABASE_NAME: helm_caio_stage1_ci
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Replay migrations on an empty database
        run: npx tsx prisma/setup-db.ts prepare

      - name: Verify Stage1 + CAIO G0 governance against MySQL
        run: npm run test:caio-stage1:mysql

  caio-pro-v1-mysql:
    name: CAIO Pro V1 Synthetic Loop MySQL
    runs-on: ubuntu-latest
    needs: [type-check, repo-guards]
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: helm_caio_pro_v1_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_pro_v1_ci?charset=utf8mb4
      CAIO_PRO_V1_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_pro_v1_ci?charset=utf8mb4
      CAIO_PRO_V1_TEST_DATABASE_NAME: helm_caio_pro_v1_ci
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Replay migrations on an empty database
        run: npx tsx prisma/setup-db.ts prepare

      - name: Verify the CAIO Pro V1 synthetic loop against MySQL
        run: npm run test:caio-pro-v1:mysql

  model-egress-mysql:
    name: Model Egress MySQL
    runs-on: ubuntu-latest
    needs: [type-check, repo-guards]
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: helm_caio_p1d_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_p1d_ci?charset=utf8mb4
      MODEL_EGRESS_STORE_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_p1d_ci?charset=utf8mb4
      MODEL_EGRESS_STORE_TEST_DATABASE_NAME: helm_caio_p1d_ci
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Replay migrations on an empty database
        run: npx tsx prisma/setup-db.ts prepare

      - name: Verify governed model egress against MySQL
        run: npm run test:model-egress:mysql

  caio-access-gateway-mysql:
    name: CAIO Access Gateway Token Store MySQL
    runs-on: ubuntu-latest
    needs: [type-check, repo-guards]
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: helm_caio_gw_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_gw_ci?charset=utf8mb4
      CAIO_ACCESS_GATEWAY_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_gw_ci?charset=utf8mb4
      CAIO_ACCESS_GATEWAY_TEST_DATABASE_NAME: helm_caio_gw_ci
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Replay migrations on an empty database
        run: npx tsx prisma/setup-db.ts prepare

      - name: Verify the CAIO access token store against MySQL
        run: npm run test:caio-access-gateway:mysql

  caio-context-memory-mysql:
    name: CAIO Context Broker and Enterprise Memory MySQL
    runs-on: ubuntu-latest
    needs: [type-check, repo-guards]
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: helm_caio_ctx_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_ctx_ci?charset=utf8mb4
      CAIO_CONTEXT_MEMORY_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_ctx_ci?charset=utf8mb4
      CAIO_CONTEXT_MEMORY_TEST_DATABASE_NAME: helm_caio_ctx_ci
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Replay migrations on an empty database
        run: npx tsx prisma/setup-db.ts prepare

      - name: Verify the context broker and enterprise memory stores against MySQL
        run: npm run test:caio-context-memory:mysql

  caio-audit-state-mysql:
    name: CAIO Audit State MySQL
    runs-on: ubuntu-latest
    needs: [type-check, repo-guards]
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: helm_caio_audit_ci
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=5s
          --health-timeout=5s
          --health-retries=20
    env:
      DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_audit_ci?charset=utf8mb4
      CAIO_AUDIT_STATE_DATABASE_URL: mysql://root:root@127.0.0.1:3306/helm_caio_audit_ci?charset=utf8mb4
      CAIO_AUDIT_STATE_TEST_DATABASE_NAME: helm_caio_audit_ci
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate

      - name: Replay migrations on an empty database
        run: npx tsx prisma/setup-db.ts prepare

      - name: Verify the CAIO audit receipt store and gate against MySQL
        run: npm run test:caio-audit-state:mysql

  quality-regression:
    name: Public Guard Regression
    runs-on: ubuntu-latest
    needs: [type-check, lint, repo-guards]
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Create example .env file
        run: cp .env.example .env

      - name: Run public guard regression
        run: npm run quality:regression

  e2e:
    name: Public Structure Smoke
    runs-on: ubuntu-latest
    needs: [type-check, lint, repo-guards]
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run public structure smoke
        run: npm run public:e2e:smoke
