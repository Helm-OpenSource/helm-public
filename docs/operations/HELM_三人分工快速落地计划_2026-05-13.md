---
status: active
owner: helm-core
created: 2026-05-13
review_after: 2026-08-11
# frontmatter backfilled by scripts/docs-frontmatter-backfill.ts on 2026-05-19
# rationale: default — no archive/dormant signal in path
---
# Helm 三人分工快速落地计划（2026-05-13）

## 今日唯一目标
今天下班前把链路恢复到可用状态：  
`信号可产生 -> 能路由到人 -> 能形成动作 -> 能被通知 -> 可复盘`

## A. 三人分工（并行推进）
| 人员 | 今日主责 | P0任务 | 今日交付（必须落系统） | 验收口径 |
|---|---|---|---|---|
| 你（Helm内核/智能设置/OPC） | 内核闭环主链 | 1) 把 BI/经营信号统一接入内核链路（signal→route→notify→action→review） 2) 智能设置加硬约束（无 owner/approver 拦截） 3) OPC 角色模板落库并启用 | 1 个内核入口（不再靠租户脚本）、1 套智能设置校验规则、1 套 OPC 角色权限模板 | 今日新信号可自动进入正式链路；critical 不允许无 owner/approver 进入 pending |
| 李健 | 生产止血（分案+路由） | 1) 修复 scheduler `LLM_ENABLED=false` 2) 对 `runId=3b47...` 全未分配批次加阻断（FAILED/REVIEW_BLOCKED） 3) 复核桥审批前校验（100% 未分配默认禁批） 4) 路由降级监控告警 | 配置修复、审批拦截逻辑、监控告警规则、该批次处理结论 | 不再出现 `llm_disabled` 批量回退；新增分案不允许“全未分配还可批准” |
| 周攀 | 数据与通道稳定性 | 1) SQLite/MySQL schema 对齐（补齐 `BiReportBusinessSignal`、`BiReportSignalNotification`） 2) 修 `BiReportRunMemory.id` NOT NULL 写入路径 3) 钉钉白名单/通知通道修复 4) 健康页加经营链路检查项 | schema 对齐脚本、写入修复、通知可用性验证、健康页新增检查项 | Signal/Notification 表可写；BI delivery 发送成功率达可用阈值；健康页能看出“经营链是否为空” |

## B. 联动依赖（避免互相等待）
1. 李健先完成 LLM 恢复与批次止血，你这边再做“动作承接与复盘”。
2. 周攀先补齐 schema，你这边再切内核统一链路。
3. 你发布智能设置硬约束后，李健的审批拦截和路由监控直接复用同一规则口径。

## C. 今日时间表（硬里程碑）
1. **12:00 前（止血）**  
   李健：`runId=3b47...` 给出“作废/重跑”结论并上系统拦截。  
   周攀：schema 对齐脚本可执行。  
   你：内核链路接入方案定稿（统一入口）。

2. **16:00 前（恢复可运行）**  
   李健：LLM 调度恢复，路由不再全落单人 fallback。  
   周攀：`BiReportRunMemory` 落库异常修复并回归通过。  
   你：智能设置硬约束 + OPC 模板上线。

3. **20:00 前（闭环验收）**  
   跑一轮真实链路：信号产生→路由→通知→动作→执行日志。  
   出当天闭环报告：发现了什么、推给谁、谁处理、是否改善。

## D. 今晚验收清单（必须全部为真）
1. `SignalEvent / ActionItem / Notification` 不再为 0（至少新增一批真实数据）。
2. 无 `llm_disabled` 导致的大规模路由降级。
3. 不允许出现“100% 未分配但审批通过”的批次。
4. 至少 1 条 critical 信号完成“动作 + 执行日志”闭环。
5. 健康页可直接显示：经营信号链是否为空、通知是否失败、路由是否降级。
